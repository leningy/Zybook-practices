import java.util.LinkedList; 
LinkedList<T> linkedList = new LinkedList<T>(); 

A class is defined to represent each list item, known as a list node. 


import java.util.Queue;
Queue<T> queue = new LinkedList<T>();
* Defines a Collection of ordered elements that supports element insertion at the tail and element retrieval from the head.

import java.util.Deque;
Deque<T> deque = new LinkedList<T>(); 
* Defines a Collection of ordered elements that supports element insertion and removal at both ends.
